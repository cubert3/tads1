# Road SOS / TADS — full dependency set (CLI + API + dashboard + tracking + optional ML)

# --- Detection & tracking (lap required by ultralytics BoT-SORT/ByteTrack) ---
ultralytics>=8.3.0
lap>=0.5.12
supervision>=0.24.0
opencv-python-headless>=4.9.0
numpy>=1.26.0
scipy>=1.11.0

# --- PyTorch (pulled by ultralytics; explicit for CLIP / GPU clarity) ---
torch>=2.0.0
torchvision>=0.15.0

# --- API server ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.9
httpx>=0.27.0
pydantic>=2.0.0
jinja2>=3.1.0

# --- Config & storage ---
pyyaml>=6.0
aiosqlite>=0.20.0

# --- Dashboard ---
streamlit>=1.35.0
plotly>=5.18.0
pandas>=2.0.0

# --- Optional pipeline features (safe to install even if disabled in yaml) ---
transformers>=4.40.0
pytesseract>=0.3.10

# --- Dev / tests ---
pytest>=8.0.0
